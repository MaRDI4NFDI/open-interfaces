if(IS_DIRECTORY lang_c)
    add_subdirectory(lang_c)
endif()
if(IS_DIRECTORY lang_julia)
    add_subdirectory(lang_julia)
endif()
if(IS_DIRECTORY lang_python)
    add_subdirectory(lang_python)
endif()
if(IS_DIRECTORY impl)
    add_subdirectory(impl)
endif()
